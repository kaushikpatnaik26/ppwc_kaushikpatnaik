#include <stdio.h>

int main() {
    int x = -123;
    printf("%u", x);
    return (0);
}
//%u is used for unassigned integer. And if the Interger is negeative the it gives a very large positive value..
