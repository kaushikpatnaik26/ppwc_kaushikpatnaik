#include <stdio.h>
int main()
{
    double length = 20, area;
    area = length * length;
    printf("Area of square the square whose side length is %.2f cm is %.2f squae cm ", length, area);
    return 0;
}
